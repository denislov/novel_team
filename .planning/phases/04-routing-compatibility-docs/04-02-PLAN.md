---
phase: 04-routing-compatibility-docs
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - plugins/novel/commands/_codex-conventions.md
  - bin/install.js
  - plugins/novel/skills/novel-command-center/SKILL.md
  - plugins/novel/skills/novel-writing/SKILL.md
  - plugins/novel/README.md
  - tests/install.test.cjs
  - tests/agent-definition.test.cjs
  - tests/codex-execution-contract.test.cjs
  - tests/codex-routing-contract.test.cjs
  - tests/readme-contract.test.cjs
autonomous: true
requirements:
  - FLOW-03
  - QUAL-01
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Codex public Novel workflows that declare `SpawnAgent(...)` either use the required named agents or stop with an explicit repair path
    - Generated Codex public skills state the exact required named agents for delegated workflows instead of only generic `spawn_agent` guidance
    - Existing install behavior and long-form compatibility remain intact while 04-02 closes execution reliability, not format-aware routing
  artifacts:
    - `plugins/novel/commands/_codex-conventions.md` contains fail-closed delegation rules for workflow-declared named agents
    - `bin/install.js` injects command-specific named-agent contract and troubleshooting text into generated Codex public skills
    - `tests/install.test.cjs`, `tests/codex-execution-contract.test.cjs`, `tests/codex-routing-contract.test.cjs`, and `tests/readme-contract.test.cjs` assert the converted Codex execution contract and public safe path
    - `tests/agent-definition.test.cjs` validates workflow metadata needed to keep named-agent execution policy explicit
    - `plugins/novel/README.md` plus the internal Codex skills explain the validation and repair path for Codex installs
  key_links:
    - Workflow-declared `<available_agent_types>` and `SpawnAgent(...)` steps are propagated into generated public Codex skill instructions
    - Public Codex skill troubleshooting points users to `node bin/install.js validate --codex --global` before substantive work begins
    - `04-01` remains the format-aware routing plan; `04-02` only hardens named-agent execution reliability
---

<objective>
Supplement Phase 4 with Codex named-agent execution hardening.

Purpose: `04-01` already covers format-aware routing and compatibility presentation. This plan closes the remaining production-safety gap where a first live Codex run can inline delegated workflow stages instead of using the named Novel sub-agents declared by the workflow.
Output: Fail-closed Codex delegation rules, command-specific generated skill contracts, converted-surface regression assertions, and Codex validation/repair documentation.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-routing-compatibility-docs/04-RESEARCH.md
@.planning/phases/04-routing-compatibility-docs/04-01-PLAN.md
@plugins/novel/README.md
@plugins/novel/commands/_codex-conventions.md
@plugins/novel/commands/write-chapter.md
@plugins/novel/workflows/write-chapter.md
@plugins/novel/workflows/review.md
@plugins/novel/workflows/plan-batch.md
@plugins/novel/workflows/autonomous.md
@plugins/novel/workflows/new-project.md
@plugins/novel/skills/novel-command-center/SKILL.md
@plugins/novel/skills/novel-writing/SKILL.md
@bin/install.js
@tests/install.test.cjs
@tests/agent-definition.test.cjs
</context>

<read_first>
Read these before implementation:
- `.planning/phases/04-routing-compatibility-docs/04-RESEARCH.md`
- `.planning/phases/04-routing-compatibility-docs/04-01-PLAN.md`
- `plugins/novel/commands/_codex-conventions.md`
- `plugins/novel/commands/write-chapter.md`
- `plugins/novel/workflows/write-chapter.md`
- `plugins/novel/workflows/review.md`
- `plugins/novel/workflows/plan-batch.md`
- `plugins/novel/workflows/autonomous.md`
- `plugins/novel/workflows/new-project.md`
- `plugins/novel/skills/novel-command-center/SKILL.md`
- `plugins/novel/skills/novel-writing/SKILL.md`
- `plugins/novel/README.md`
- `bin/install.js`
- `tests/install.test.cjs`
- `tests/agent-definition.test.cjs`
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Fail closed for workflow-declared named agents in Codex</name>
  <requirements>FLOW-03, QUAL-01, QUAL-02</requirements>
  <files>plugins/novel/commands/_codex-conventions.md, bin/install.js</files>
  <read_first>plugins/novel/commands/_codex-conventions.md, bin/install.js, plugins/novel/workflows/write-chapter.md, plugins/novel/workflows/review.md, plugins/novel/workflows/plan-batch.md, plugins/novel/workflows/autonomous.md, plugins/novel/workflows/new-project.md, tests/install.test.cjs</read_first>
  <action>Replace the permissive `_codex-conventions.md` fallback sentence `Only fall back to inline execution if named agents are unavailable or the workflow is trivially small.` with a fail-closed rule: if a public Novel workflow declares `SpawnAgent(...)`, the parent Codex surface must use the exact named agent or agents, or stop with actionable repair guidance. In `bin/install.js`, extend the Codex adapter-generation path so `convertNovelCommandToCodexSkill()` derives required named agents from the routed workflow and emits command-specific contract text for at least `$novel-write-chapter`, `$novel-review`, `$novel-plan-batch`, `$novel-autonomous`, and `$novel-new-project`. The generated contract must state the exact required named agents, say delegated stages must not be inlined, and include a remediation path that surfaces `node bin/install.js validate --codex --global` before any substantive inline work. Preserve existing install layout, public skill names, internal-skill hiding, and the format-aware routing behavior already handled by `04-01`.</action>
  <verify>node --test tests/install.test.cjs tests/agent-definition.test.cjs</verify>
  <acceptance_criteria>
    - `plugins/novel/commands/_codex-conventions.md` no longer contains `Only fall back to inline execution if named agents are unavailable or the workflow is trivially small.`
    - `bin/install.js` contains strong contract language such as `do not inline delegated stages` and `validate --codex --global`
    - A generated `$novel-write-chapter` skill contract names `novel-planner`, `novel-writer`, `novel-editor`, and `novel-verifier`
    - A generated `$novel-new-project` skill contract names `novel-architect` and handles `novel-researcher` as an explicit conditional or required step rather than a silent inline fallback
  </acceptance_criteria>
  <done>Codex public skills fail closed for workflow-declared named-agent stages instead of silently inlining them.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Codex runtime reliability contract in internal skills and README</name>
  <requirements>FLOW-03, QUAL-03</requirements>
  <files>plugins/novel/skills/novel-command-center/SKILL.md, plugins/novel/skills/novel-writing/SKILL.md, plugins/novel/README.md</files>
  <read_first>plugins/novel/skills/novel-command-center/SKILL.md, plugins/novel/skills/novel-writing/SKILL.md, plugins/novel/README.md, plugins/novel/commands/_codex-conventions.md, .planning/phases/04-routing-compatibility-docs/04-01-PLAN.md</read_first>
  <action>Update the internal Codex skills and README so they explain that workflows with declared `SpawnAgent(...)` stages depend on installed named agents and must not silently inline those stages. Add a concise troubleshooting path that tells the user how to validate and repair a Codex install with `node bin/install.js validate --codex --global`, followed by reinstall or update if validation fails or named-agent execution drifts. Keep the wording explicit that `04-01` already handled format-aware routing and compatibility presentation; this supplemental plan is about execution reliability of named agents in Codex and must preserve the long-form compatibility baseline.</action>
  <verify>rg -n "named agents|do not inline|validate --codex --global|execution reliability|format-aware routing" plugins/novel/README.md plugins/novel/skills/novel-command-center/SKILL.md plugins/novel/skills/novel-writing/SKILL.md</verify>
  <acceptance_criteria>
    - `plugins/novel/README.md` contains a dedicated Codex reliability contract and repair path
    - `plugins/novel/skills/novel-command-center/SKILL.md` tells the router to honor workflow-declared named agents rather than absorbing delegated work inline
    - `plugins/novel/skills/novel-writing/SKILL.md` reinforces that Codex command entry should respect required named-agent stages instead of replacing them with direct parent execution
  </acceptance_criteria>
  <done>User-facing and internal Codex guidance explain the execution contract and how to validate or repair it.</done>
</task>

<task type="auto">
  <name>Task 3: Add converted-surface regression tests for mandatory agent contracts</name>
  <requirements>QUAL-01, QUAL-02</requirements>
  <files>tests/install.test.cjs, tests/agent-definition.test.cjs, tests/codex-execution-contract.test.cjs, tests/codex-routing-contract.test.cjs, tests/readme-contract.test.cjs</files>
  <read_first>tests/install.test.cjs, tests/agent-definition.test.cjs, bin/install.js, plugins/novel/commands/_codex-conventions.md, plugins/novel/commands/write-chapter.md, plugins/novel/workflows/write-chapter.md, plugins/novel/workflows/review.md, plugins/novel/workflows/plan-batch.md, plugins/novel/workflows/autonomous.md, plugins/novel/workflows/new-project.md, plugins/novel/README.md, plugins/novel/skills/novel-command-center/SKILL.md, plugins/novel/skills/novel-writing/SKILL.md</read_first>
  <action>Extend `tests/install.test.cjs` beyond generic `SpawnAgent() → spawn_agent Mapping` presence checks. Add new file `tests/codex-execution-contract.test.cjs` to assert representative generated public Codex skills include exact required named-agent lists, fail-closed wording such as `do not inline delegated stages`, and validation or repair guidance. Add new file `tests/codex-routing-contract.test.cjs` to assert the supported Codex public safe path remains the explicit `$novel-*` skills and that internal router exposure does not drift into the public surface. Add new file `tests/readme-contract.test.cjs` to assert README distinguishes install correctness from execution correctness and documents the repair path. Extend `tests/agent-definition.test.cjs` so workflows that declare named agents also expose explicit execution-policy metadata or equivalent contract signals needed by the converted Codex surface. Preserve existing install assertions for support bundle structure, hidden internal skills, agent TOML generation, and the format-aware artifacts already protected by `04-01`.</action>
  <verify>node --test tests/install.test.cjs tests/agent-definition.test.cjs tests/codex-execution-contract.test.cjs tests/codex-routing-contract.test.cjs tests/readme-contract.test.cjs</verify>
  <acceptance_criteria>
    - `tests/install.test.cjs` asserts more than the generic `SpawnAgent() → spawn_agent Mapping` heading
    - `tests/codex-execution-contract.test.cjs` exists and checks exact agent names for at least `novel-write-chapter` and `novel-new-project`
    - `tests/codex-routing-contract.test.cjs` exists and checks the explicit public `$novel-*` safe path
    - `tests/readme-contract.test.cjs` exists and checks installation-vs-execution wording plus repair guidance
    - `tests/agent-definition.test.cjs` rejects workflows that hide named-agent execution policy implicitly
    - The test suite rejects permissive inline-fallback wording in installed Codex conventions or generated skills
    - `node --test tests/install.test.cjs tests/agent-definition.test.cjs tests/codex-execution-contract.test.cjs tests/codex-routing-contract.test.cjs tests/readme-contract.test.cjs` exits 0
  </acceptance_criteria>
  <done>The converted Codex surface is regression-protected at the orchestration-contract level, not just the syntax level.</done>
</task>

</tasks>

<acceptance_criteria>
- `04-01` remains the format-aware routing plan and is not edited or replaced
- `rg -n "do not inline delegated stages|validate --codex --global|novel-planner|novel-writer|novel-editor|novel-verifier|novel-architect" bin/install.js plugins/novel/commands/_codex-conventions.md tests/install.test.cjs plugins/novel/README.md plugins/novel/skills/novel-command-center/SKILL.md plugins/novel/skills/novel-writing/SKILL.md` returns matches
- `rg -n "Only fall back to inline execution if named agents are unavailable or the workflow is trivially small\\." plugins/novel/commands/_codex-conventions.md` returns no matches
- Existing long-form compatibility and install behavior remain protected by the unchanged public skill list and passing install tests
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user request -> Codex public skill | Broad user intent can tempt the parent skill to satisfy work inline instead of honoring workflow-declared delegation |
| generated public skill -> named sub-agents | The converted surface must preserve the workflow's required agent contract |
| installer validation -> local Codex runtime | Missing or drifted agent registration must become an explicit stop condition with repair guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | `plugins/novel/commands/_codex-conventions.md` | mitigate | Remove permissive inline fallback for workflow-declared delegated stages |
| T-04-02-02 | I | `bin/install.js` generated public skills | mitigate | Inject exact required agent lists, fail-closed wording, and validation guidance into converted Codex skills |
| T-04-02-03 | D | Codex runtime agent registration | mitigate | Surface `node bin/install.js validate --codex --global` and reinstall guidance before work continues |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/install.test.cjs tests/agent-definition.test.cjs` passes
- [ ] `node --test tests/install.test.cjs tests/agent-definition.test.cjs tests/codex-execution-contract.test.cjs tests/codex-routing-contract.test.cjs tests/readme-contract.test.cjs` passes
- [ ] `rg -n "do not inline delegated stages|validate --codex --global" bin/install.js plugins/novel/commands/_codex-conventions.md` returns matches
- [ ] `rg -n "novel-planner|novel-writer|novel-editor|novel-verifier|novel-architect|novel-researcher" tests/install.test.cjs bin/install.js` returns matches
- [ ] `rg -n "execution reliability|format-aware routing" plugins/novel/README.md plugins/novel/skills/novel-command-center/SKILL.md plugins/novel/skills/novel-writing/SKILL.md` returns matches
</verification>

<success_criteria>
- All tasks completed
- Codex public skills enumerate required named agents and fail closed for delegated workflow stages
- Users get a visible validation and repair path instead of silent inline degradation
- Regression tests protect the converted Codex surface, public safe path, README contract, and named-agent execution policy
- `04-01` remains the Phase 4 format-aware routing plan; `04-02` only closes named-agent execution reliability
</success_criteria>

<output>
After completion, create `.planning/phases/04-routing-compatibility-docs/04-02-SUMMARY.md`
</output>
