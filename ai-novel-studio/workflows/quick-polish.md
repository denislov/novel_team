<purpose>
快速润色别名：复用 polish 工作流，但默认采用 quick 模式，优先处理明显的 AI 味、节奏和语句问题。
</purpose>

<required_reading>
Read `workflows/polish.md` before starting.
</required_reading>

<process>
Load and execute `workflows/polish.md` end-to-end.

Additional constraints for this alias:
- 默认行为等价于 `polish --quick`。
- 优先最小修改，不主动扩写或重构章节结构，除非原文已明显失真。
- 保持 `polish` 既有的产物路径和接受流程。
</process>
